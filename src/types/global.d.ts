// import { Request, Response, NextFunction } from 'express';

// declare global {
//    interface AppRequest extends Request {
//       user?: any;
//    }

//    type Controller = (req: AppRequest, res: Response, next: NextFunction) => any;
// }
